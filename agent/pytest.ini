[pytest]
addopts = -m "not slow" --basetemp=.pytest-tmp
tmp_path_retention_count = 3
markers =
    slow: vollständige Pipeline-Läufe auf Baseline-PDFs (~15-30 min pro PDF).
          Nur mit pytest -m slow ausführen, nicht im normalen Entwicklungs-Loop.
